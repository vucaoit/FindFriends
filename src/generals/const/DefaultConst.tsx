import { Dimensions } from 'react-native';
import { Screen } from 'react-native-screens';
export const defaultIconSize = 24;
export const defaultHorizontalPadding = 16;
export const defaultCornerRadius = 16;
export const defaultFontSize = 16;
export const defaultWidth = Dimensions.get('screen').width;
export const defaultHeight = Dimensions.get('screen').height;
